// Placeholder until the committee's photos and roles are ready. Replace the
// contents of <div className="team-page"> with the real thing — the banner
// above it is the same one every section page uses, so it will already match.
export default function MeetTheTeam() {
  return (
    <>
      <div className="section-hero" style={{ '--accent': 'var(--white)', '--accent-ink': 'var(--black)' }}>
        <h1 className="display">Meet the Team</h1>
      </div>

      <div className="team-page">
        <p>Will be revealed soon!</p>
      </div>
    </>
  )
}
